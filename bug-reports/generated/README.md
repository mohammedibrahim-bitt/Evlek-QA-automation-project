# Generated Bug Drafts

Playwright writes generated failure drafts here after each test run:

- `latest-failures.csv`
- `latest-failures.md`

These files are ignored from Git because they are run-specific evidence. Use them as a starting point, then copy confirmed product issues into `bug-reports/bugs.csv` and a permanent `BUG-###-short-title.md` report.
